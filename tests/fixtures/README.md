# Test fixtures

Shared in-memory repositories, fake providers, fake DNS resolvers, and app factories live in `tests/conftest.py` so flat legacy tests and the Phase 11 test pyramid can use the same fixtures.
